# =============================================================================
# JobPulse — Python Dependencies
# Generated for Python 3.12
# =============================================================================
# Install core dependencies:      pip install -r requirements.txt
# Install with dev tools:         pip install -e ".[dev]"
# =============================================================================

# -----------------------------------------------------------------------------
# Core Data Processing
# -----------------------------------------------------------------------------
pandas>=2.2.0           # DataFrame-based data manipulation
numpy>=1.26.0           # Numerical computing (Pandas dependency)

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
sqlalchemy>=2.0.0       # ORM + Core SQL abstraction layer
psycopg2-binary>=2.9.9  # PostgreSQL adapter for Python
alembic>=1.13.0         # Database migration management

# -----------------------------------------------------------------------------
# Configuration & Environment
# -----------------------------------------------------------------------------
python-dotenv>=1.0.0    # Load .env files into os.environ
pyyaml>=6.0.1           # YAML configuration file parsing
pydantic>=2.5.0         # Data validation using Python type hints
pydantic-settings>=2.1.0 # Settings management with Pydantic v2

# -----------------------------------------------------------------------------
# HTTP / API Clients & Servers
# -----------------------------------------------------------------------------
requests>=2.31.0        # HTTP library for API calls
httpx>=0.26.0           # Async HTTP client
fastapi>=0.100.0        # Fast web framework for backend API
uvicorn[standard]>=0.23.0 # ASGI server for FastAPI

# -----------------------------------------------------------------------------
# Resilience & Retry Logic
# -----------------------------------------------------------------------------
tenacity>=8.2.3         # Retry library with exponential backoff

# -----------------------------------------------------------------------------
# Logging & Monitoring
# -----------------------------------------------------------------------------
# (Python stdlib logging is used; structlog added for structured logs)
structlog>=24.1.0       # Structured, contextual logging

# -----------------------------------------------------------------------------
# Data Validation (future Great Expectations integration)
# -----------------------------------------------------------------------------
# great-expectations>=0.18.0   # Uncomment when validation layer is built

# =============================================================================
# Development & Testing Dependencies
# Install with: pip install -e ".[dev]"
# =============================================================================
# pytest>=7.4.0
# pytest-cov>=4.1.0
# pytest-mock>=3.12.0
# black>=23.12.0
# ruff>=0.1.9
# mypy>=1.8.0
# pre-commit>=3.6.0
# ipython>=8.20.0
# Orchestration
apache-airflow>=2.10.0
apache-airflow-providers-postgres>=2.0.0
gunicorn>=21.2.0
pyarrow
fastparquet

# -----------------------------------------------------------------------------
# Frontend (Streamlit)
# -----------------------------------------------------------------------------
streamlit>=1.30.0
plotly>=5.18.0
streamlit-aggrid>=1.0.0
streamlit-option-menu>=0.3.6
streamlit-extras>=0.3.5
Pillow>=10.2.0
altair>=5.2.0
streamlit-lottie>=0.0.3
